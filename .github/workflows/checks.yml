name: checks

on:
  push:
  pull_request:

jobs:
  package:
    name: credentials and estate identifiers
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan tracked files
        run: git ls-files -z | xargs -0 ./tools/check-package.py
